package me.dacol.marco.mancala.gameLib.gameController.actions;

import me.dacol.marco.mancala.gameLib.player.Player;

public class Winner extends Action<Player> {

    public Winner(Player load) {
        super(load);
    }
}
