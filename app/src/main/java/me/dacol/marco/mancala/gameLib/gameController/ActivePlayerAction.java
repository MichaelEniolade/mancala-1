package me.dacol.marco.mancala.gameLib.gameController;

import me.dacol.marco.mancala.gameLib.player.Player;

public class ActivePlayerAction extends Action<Player> {

    protected ActivePlayerAction(Player load) {
        super(load);
    }

}
